default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  port: 5432
  username: postgres
  password: postgres
  pool: 5

development:
  <<: *default
  database: milkdown_engine_development

test:
  <<: *default
  database: milkdown_engine_test
