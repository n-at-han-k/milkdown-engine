{
  "name": "milkdown-engine",
  "private": true,
  "type": "module",
  "description": "Build tooling for milkdown_engine Rails engine",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "dependencies": {
    "@milkdown/crepe": "^7.8.0",
    "@milkdown/kit": "^7.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
